function init()
chrome.browserAction.onClicked.addListener(funtion (tab){
});
document.getElementById("button").onClick = function getLastOne();
    chrome.storage.sync.clear();
chrome.commands.onCommand.addListener(function (onClick){
call()
});
chrome.tabs.onUpdated.addListener(function (tabId, changeInfo, tab)
{
localStorage["tabTitle-" + tabId] = tabUrl;
if (tab.title != null)
    localStorage["tabTitle-"+tabId] = tabTitle;
if (tab.title !=null)
    localStorage["tabTitle-"+tabId] = tabUrl;
});

chrome.tabs.onReplaced.addListener(newTabId, oldTabId) {
chrome.tabs.getNewTabId, function (tab){
removedUrl = tab:url;
});
});
chrome.tabs.onRemoved.addListener(function (tabID, removeInfo) {
console.log("***onRemoved***" + tabId);
if (typeof removedTabId !="undefined")
{ lastRemovedId = removedTabId;}
removedTabId = tabId;
console.log("lastRemoved" + lastRemovedId + "recently removed" + removeTabId);
   optimise();
function getLastOne()
{
var returnString = <html><body>;
for (i = localStorage["closeCount"]-i; 1 >=0; i--)
{
